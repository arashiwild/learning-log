# Docker Basic Commands

```bash
docker build -t myapp .
docker run -p 3000:3000 myapp
docker ps
docker stop <container_id>
docker images
docker rmi <image_id>
```

Docker membantu menjalankan aplikasi dalam container yang konsisten di semua environment.
