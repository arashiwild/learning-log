# Accessibility (a11y) Dasar

- Gunakan HTML semantik (`<button>`, `<nav>`, `<header>`)
- Tambahkan `alt` text pada gambar
- Pastikan kontras warna cukup
- Pastikan navigasi bisa dilakukan via keyboard
- Gunakan ARIA attribute jika perlu, tapi semantik HTML lebih diutamakan
