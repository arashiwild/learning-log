# Big O Notation

Mengukur kompleksitas algoritma terhadap ukuran input (n).

- O(1): constant time
- O(log n): logarithmic (binary search)
- O(n): linear (loop sederhana)
- O(n log n): sorting efisien (merge sort)
- O(n^2): nested loop
- O(2^n): exponential (brute force subset)
