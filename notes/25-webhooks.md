# Webhooks

Webhook adalah cara satu sistem memberi notifikasi ke sistem lain secara real-time via HTTP POST saat event terjadi.

Contoh: GitHub webhook mengirim payload ke server kita setiap ada push baru, lalu server bisa trigger CI/CD.
