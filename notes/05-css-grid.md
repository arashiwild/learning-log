# CSS Grid Dasar

Grid digunakan untuk layout 2 dimensi (baris dan kolom).

```css
.container {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
}
```

Cocok untuk layout dashboard, galeri, dan card grid.
